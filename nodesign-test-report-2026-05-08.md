# Nodesign UX 报告（问题版）

- 测试对象：`https://nodesign.xiaobuyu.trade`
- 测试日期：2026-05-08

---

## 结论先行

当前主要问题集中在：**排版质量、页面边界可识别性、生成稳定性、分享可达性**。  
除上述问题外，其它已通过项不在本报告展开。

---

## 问题与建议（仅保留异常项）

### 1) 排版与占比问题（高频）

- 现象：部分页面内容明显偏左/偏上，右侧或下半区空白较大；存在“未居中”的视觉感受。
- 影响：页面利用率低，演示时观感不够饱满。
- 建议：引入固定栅格（`50/50` 或 `40/60`），并增加每页最小内容覆盖率阈值（建议 >= 70%）。

### 2) 页面边界识别问题（编辑态）

- 现象：页面背景与画布底色接近时，未选中状态下很难看出页面边界。
- 影响：用户不易判断当前编辑区域，拖慢修改效率。
- 建议：编辑态增加明确边界样式（描边/阴影/对比底色），区分“页面本体”和“编辑画布”。

### 3) 生成链路稳定性问题

- 现象：会话内多次出现“长时间无新增输出”，且右侧画布偶发空白，需要手动“重载 iframe”。
- 影响：用户难以判断系统是否仍在有效生成，打断创作节奏。
- 建议：增加可见阶段状态（生成中/排版中/渲染中）与超时自动恢复机制。

### 4) 分享外链可达性问题

- 现象：`nodesign.app` 分享链接在测试中出现不可访问情况。
- 影响：对外评审与协作链路受阻。
- 建议：发布前加入分享域名可达性检查，异常时提供回退链接或错误提示。

### 5) iframe/sandbox 运行时错误

- 现象：控制台持续出现 `Blocked script execution` 与 `allow-scripts + allow-same-origin` 相关报错。
- 影响：渲染稳定性与安全感知受影响。
- 建议：统一 iframe sandbox 策略，减少运行时噪音和重复报错。

#### 复现场景（步骤级）

1. 首页加载最近项目缩略预览时。
2. 进入会话页渲染右侧画布时。
3. 点击“重载 iframe”后再次渲染时（如 `canvas?v=1`）。
4. 切换到 `/work` 页面渲染时。

---

## 截图（排版与边界问题示例）

PPT排版与页面边界问题示例

若本地 Markdown 预览不显示图片，可改为 Canvas 版提交。